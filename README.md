## Core parts explanation:

1. index.php - routes get request to proper controllers and router methods
2. api.php - routers api calls to proper api's from api/
3. controllers/ - each controller is a class that has methods, which may prepare/get data from models/ and load html elements from views/


## Notes

> headers, footers, navigation (side bars) are imported/required inside individual views/pages